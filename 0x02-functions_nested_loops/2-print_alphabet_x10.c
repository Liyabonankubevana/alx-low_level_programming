include "main.h"

/**
 * print_alphabet_x10 -> Prints the lowercase alphabets
 */
	
void print_alphabet_x10(void)
{
	int = j;
	int = x;

	for (x = 0; x <= 10; x++)
	{
		for (x = 'a'; j <= z; j++)
		{
			_putchar(j);
		}
		_putchar('\n');
	}
}
